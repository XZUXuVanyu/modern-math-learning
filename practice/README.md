# Computational practice

This replaces the source project's practice-plugin directory. Create a lesson-specific subdirectory only when its single homework is computational. Prefer C++ given the learner's background.

The learner writes the crucial implementation and validation reasoning. Record equations, assumptions, commands, actual results, and numerical limitations alongside code. Code is optional for this mathematics course and is not needed for M001. No executable implementation or build system has been created.
